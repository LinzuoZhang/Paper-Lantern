Task:
Correct only the formatting of this Method Breakdown JSON for rendering. Do not add new facts.

Method Breakdown JSON:
{method_json}

Hard formatting rules:
- Do not output Markdown outside JSON.
- Preserve the existing Chinese wording and structure as much as possible.
- Do not restructure any section's prose into a bullet list or a separate formula list, and do not merge or split sections.
- Formula markup is mandatory inside each section's content: block formulas must be wrapped in "$$...$$"; inline formulas or short notations must be wrapped in "$...$". Fix a formula that is missing its delimiters only when the surrounding wording makes the math boundary unambiguous.
- Each block formula ("$$...$$") must stand on its own line: put a blank line above and below the formula inside the content value, and do not leave surrounding text glued to it on the same line.
- Clearly mathematical notation that the model wrote as plain Unicode text must still be wrapped in math delimiters. Without changing the meaning, convert obvious Unicode math into simple LaTeX and wrap inline expressions in "$...$": for example, μ to $\mu$, σ to $\sigma$, Φ to $\Phi$, ξ to $\xi$, δ to $\delta$, ℓ to $\ell$, ‖ to $\|$, ∼ to $\sim$, ∏ to $\prod$, · to $\cdot$, and wrap any subscripts/superscripts in the same way. Wrap only expressions that are clearly mathematics; do not wrap Chinese words or the parenthesized domain-term glosses.
- Symbol explanations must stay as plain Chinese text inside the content, after the formula; never move them inside "$...$" or "$$...$$".
- For multi-character subscripts, braces are mandatory: write $P_{{target}}$, $p_{{current}}$, and $c_{{obs}}$ rather than $P_target$, $p_current$, or $c_obs$.
- Do not change domain terms except to preserve the required "中文解释 (Original English Full Term, ABBR)" or "中文解释 (Original English Full Term)" format when already present.
- When mentioning domain-specific terms, model/module names, algorithms, metrics, datasets, or coined concepts, the mention must be a complete phrase in the form "Chinese explanation (Original English Full Term, ABBR)" when an abbreviation/acronym exists, or "Chinese explanation (Original English Full Term)" when it does not. Do not use only the Chinese translation. Use the exact original English term from the paper when available.
